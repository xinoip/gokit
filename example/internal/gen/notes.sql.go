// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: notes.sql

package gen

import (
	"context"

	"github.com/google/uuid"
)

const notesCreate = `-- name: NotesCreate :one
INSERT INTO notes (
    note_id,
    title,
    body
) VALUES (
    $1,
    $2,
    $3
) RETURNING note_id, title, body, created_at, updated_at
`

type NotesCreateParams struct {
	NoteID uuid.UUID `json:"noteId"`
	Title  string    `json:"title"`
	Body   string    `json:"body"`
}

func (q *Queries) NotesCreate(ctx context.Context, arg NotesCreateParams) (Notes, error) {
	row := q.db.QueryRow(ctx, notesCreate, arg.NoteID, arg.Title, arg.Body)
	var i Notes
	err := row.Scan(
		&i.NoteID,
		&i.Title,
		&i.Body,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const notesDelete = `-- name: NotesDelete :exec
DELETE FROM notes WHERE note_id = $1
`

func (q *Queries) NotesDelete(ctx context.Context, noteID uuid.UUID) error {
	_, err := q.db.Exec(ctx, notesDelete, noteID)
	return err
}

const notesGet = `-- name: NotesGet :one
SELECT note_id, title, body, created_at, updated_at FROM notes WHERE note_id = $1
`

func (q *Queries) NotesGet(ctx context.Context, noteID uuid.UUID) (Notes, error) {
	row := q.db.QueryRow(ctx, notesGet, noteID)
	var i Notes
	err := row.Scan(
		&i.NoteID,
		&i.Title,
		&i.Body,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const notesList = `-- name: NotesList :many
SELECT note_id, title, body, created_at, updated_at FROM notes ORDER BY created_at DESC
`

func (q *Queries) NotesList(ctx context.Context) ([]Notes, error) {
	rows, err := q.db.Query(ctx, notesList)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Notes{}
	for rows.Next() {
		var i Notes
		if err := rows.Scan(
			&i.NoteID,
			&i.Title,
			&i.Body,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const notesUpdate = `-- name: NotesUpdate :one
UPDATE notes
SET
    title = $1,
    body = $2,
    updated_at = CURRENT_TIMESTAMP
WHERE note_id = $3
RETURNING note_id, title, body, created_at, updated_at
`

type NotesUpdateParams struct {
	Title  string    `json:"title"`
	Body   string    `json:"body"`
	NoteID uuid.UUID `json:"noteId"`
}

func (q *Queries) NotesUpdate(ctx context.Context, arg NotesUpdateParams) (Notes, error) {
	row := q.db.QueryRow(ctx, notesUpdate, arg.Title, arg.Body, arg.NoteID)
	var i Notes
	err := row.Scan(
		&i.NoteID,
		&i.Title,
		&i.Body,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
